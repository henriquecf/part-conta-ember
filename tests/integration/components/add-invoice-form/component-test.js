import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('add-invoice-form', 'Integration | Component | add invoice form', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
