{{grouped-invoices list=model groupByField="category" cardTitle="Despesas por categoria"}}

{{grouped-invoices list=model groupByField="category" isRevenue=true cardTitle="Ganhos por categoria"}}

{{#if hasManyGroups}}
  {{grouped-invoices list=model groupByField="group" fieldName="group.name" cardTitle="Despesas por grupo"}}
  
  {{grouped-invoices list=model groupByField="group" fieldName="group.name" isRevenue=true cardTitle="Ganhos por grupo"}}
{{/if}}

{{#if hasManyUsers}}
  {{grouped-invoices list=model groupByField="user" fieldName="user.name" cardTitle="Despesas por usuário"}}
  
  {{grouped-invoices list=model groupByField="user" fieldName="user.name" isRevenue=true cardTitle="Ganhos por usuário"}}
{{/if}}

{{grouped-invoices list=model groupByField="description" showGraph=false listSize=5 cardTitle="Principais despesas"}}

{{grouped-invoices list=model groupByField="description" showGraph=false listSize=5 isRevenue=true cardTitle="Principais ganhos"}}
